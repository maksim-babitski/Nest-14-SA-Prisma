# Next 14

Connecting a database to a NextJS application using SQLite and Prisma as an example. Adding server actions with queries to the database.

For start:

1. nmp install
2. npm run prisma-seed (optional, for initiation od database)
3. npx prisma studio
4. npm run dev
